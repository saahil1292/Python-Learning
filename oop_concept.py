#!/usr/bin/python2.7

class CurrencyGenerator(object):

    def Currency(self):
        currency = ['Dollar', 'Euro', 'Rupees', 'Yen', 'Pound', 'Ringet']
        print 'Enter your amount to convert?'
        cur = raw_input(': ')
        
    def Converter(self):
        for cur in currency:
            if (cur == 'Dollar'):
                print 'which dollar is it?'
                if (cur == '
